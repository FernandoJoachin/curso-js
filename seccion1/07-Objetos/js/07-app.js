const producto = {
    nombre: "Monitor curvo",
    precio: 1000,
    disponible: true
}

//Reescribir el valor de un producto (si se puede aunque sea una constante)
producto.disponible = false;
console.log(disponible);